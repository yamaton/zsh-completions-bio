#compdef _transmute transmute

# Auto-generated with h2o


function _transmute {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-x2p[Reformat XML]' \
        '-j2p[Reformat JSON]' \
        '-align[-a codes Column alignment codes:]' \
        '-j2x[Convert JSON stream to XML suitable for -path navigation. -set tag]' \
        '-a2x[Convert text ASN.1 stream to XML suitable for -path navigation. -set tag Replace set wrapper tag. -rec tag Replace record wrapper tag.]' \
        {-t2x,-c2x}'[Convert tab-delimited table or comma-separated values file, respectively, to XML. -set tag Replace set wrapper tag. -rec tag Replace record wrapper tag. -skip N Skip the first N lines. -header Use fields from first row for column names. -lower Convert text to lowercase. -upper Convert text to uppercase. -indent Indent XML output. -flush Do not indent XML output. columnName1 ... XML object names per column.]' \
        '-g2x[Convert GenBank flatfile format to INSDSeq XML.]' \
        '-diff[Compare two aligned files for point differences.]' \
        '-extrac[t feat_loc]' \
        '-hgvs[Convert Human Genome Variation Society variation format to XML.]' \
        '-aa1to3[Convert amino acids from 1-character to 3-character format.]' \
        '-aa3to1[Convert amino acids from 3-character to 1-character format.]' \
        '-format[copy Fast block copy (still applies processing flags). compact Compress runs of spaces. flush Suppress line indentation. indent Indent according to nesting depth. expand Place each attribute on a separate line.]' \
        '-filter[Actions: retain Keep matching elements (no-op). remove Remove matching elements. encode HTML-escape special characters. decode Decode HTML escapes. shrink Compress runs of spaces. expand Place each attribute on a separate line. accent Strip off Unicode accents.]' \
        '-normalize[Adjust XML fields to conform to common conventions.]' \
        '-revcomp[Reverse complement nucleotide sequence.]' \
        '-remove[Trim at ends of sequence.]' \
        '-retain[Save either end of sequence.]' \
        '-replace[Apply base or residue substition.]' \
        '-molwt[Calculate molecular weight of peptide.]' \
        '-encodeXML[XML-encode <, >, &, ", and '\'' characters.]' \
        '-decodeXML[Decode XML entity references.]' \
        '-plainXML[Remove embedded mixed-content tags and compress runs of spaces.]' \
        '-encodeURL[Compress runs of spaces, and URI-escape the result.]' \
        '-decodeURL[URI-unescape the input.]' \
        '-encode64[Base64-encode the input.]' \
        '-decode64[Base64-decode the input.]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

