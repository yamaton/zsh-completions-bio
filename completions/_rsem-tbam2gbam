#compdef _rsem-tbam2gbam rsem-tbam2gbam

# Auto-generated with h2o


function _rsem-tbam2gbam {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-p[number of threads which user wants to use. (default: 1)]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

