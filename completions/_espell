#compdef _espell espell

# Auto-generated with h2o


function _espell {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-help[Print usage information.]' \
        '-db[Entrez database name.]' \
        '-q[Query string.]' \
        '-query[Query string.]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

