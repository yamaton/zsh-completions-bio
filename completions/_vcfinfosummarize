#compdef _vcfinfosummarize vcfinfosummarize

# Auto-generated with h2o


function _vcfinfosummarize {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        {-f,--field}'[Summarize this field in the INFO column]' \
        {-i,--info}'[Store the computed statistic in this info field]' \
        {-a,--average}'[Take the mean for field (default)]' \
        {-m,--median}'[Use the median]' \
        {-n,--min}'[Use the min]' \
        {-x,--max}'[Use the max]' \
        {-h,--help}'[Print this message]' \
        {-v,--version}'[Print version]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

