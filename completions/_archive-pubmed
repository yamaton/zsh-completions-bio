#compdef _archive-pubmed archive-pubmed

# Auto-generated with h2o


function _archive-pubmed {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-path[Place the local archive in dir.]':file:_files \
        {-temp,-work,-working}'[Place intermediate subtrees in dir.]':file:_files \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

