#compdef _edirect edirect

# Auto-generated with h2o


function _edirect {
    local line state

    function _commands {
        local -a commands
        commands=(
        )
        _describe 'command' commands
    }
 

    _arguments -C \
        '-help[Print usage information.]' \
        '-email[Send the given e-mail address to have someone to notify in case a runaway script causes problems with an NCBI Entrez server.]' \
        '-tool[Send the given client name.]' \
        '-http[Force the use of GET rather than POST HTTPS requests.]' \
        '-https[Force the use of GET rather than POST HTTPS requests.]' \
        '-alias[Specify a file of shortcut keywords and query strings or URL sections.]':file:_files \
        '-silent[Suppress connection failure retry messages.]' \
        '-verbose[Display the ENTREZ_DIRECT XML field values at each step.]' \
        '-debug[Print the internal URL query and XML results of each step.]' \
        '-base[Specify a particular server for quality assurance testing.]' \
        '*: :_files'

    case $state in
    (cmd)
        _commands
        ;;
    (subcmd)
        case $line[1] in
        esac
        ;;
     esac

}

